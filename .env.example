# Backend API Configuration
# Change these URLs to match your backend server

# Basically in all environment variables you need to paste the backend URL

# Base URL for the backend server (Vite format)
VITE_API_URL=http://localhost:3000

# Socket.IO server URL (same as the backend server)
VITE_SOCKET_URL=http://localhost:3000

# Legacy support for REACT_APP_ prefix (same as the backend server)
REACT_APP_API_URL=http://localhost:3000
REACT_APP_SOCKET_URL=http://localhost:3000

# Firebase Configuration
# Note: Firebase configuration files and folders are typically hidden
# Check /utils directory for firebase-related folders
# Download the Firebase configuration from your Firebase console and place it in the appropriate location
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

NODE_ENV=development # Environment mode
# Set to 'production' for production builds
# Set to 'development' for development builds